# ============================================================
# MJPEG HTTP Streamer – Base configuration template
#
# This file is a template.
# It will be copied to config.env and enriched automatically
# by install_service.sh with system-specific values.
# ============================================================

# ------------------------------------------------------------
# Application entrypoint (required)
# ------------------------------------------------------------

# Uvicorn application import string
# Format: <python_module>:<fastapi_app>
APP_MODULE=mjpeg_server:app


# ------------------------------------------------------------
# Network configuration
# ------------------------------------------------------------

# Bind address for the HTTP server
HOST=0.0.0.0

# TCP port for the MJPEG stream
PORT=8000


# ------------------------------------------------------------
# Stream behaviour
# ------------------------------------------------------------

# Target streaming FPS (applied to both streams)
TARGET_FPS=20.0

# Maximum age (in seconds) for frames on disk (applied to both streams)
MAX_FRAME_AGE_S=10.0


# ------------------------------------------------------------
# Frames directories (two independent sources)
# ------------------------------------------------------------

# Absolute path where producer writes frames for source 1.
#
# If left empty, install_service.sh will set it to:
#   <MJPEG_HTTP_STREAMER_DIR>/images1
#
FRAMES_OJBECT_DIR_ABS=/home/alexios/Projects/deliver/test_results/10_02/192.168.200.200/RGB/detector

# Absolute path where producer writes frames for source 2.
#
# If left empty, install_service.sh will set it to:
#   <MJPEG_HTTP_STREAMER_DIR>/images2
#
FRAMES_ROCK_DIR_ABS=/home/alexios/Projects/deliver/test_results/10_02/192.168.200.200/RGB/rocks


# ------------------------------------------------------------
# Values injected automatically by install_service.sh
# (DO NOT set manually here)
# ------------------------------------------------------------

INVOKING_USER=
MJPEG_HTTP_STREAMER_DIR=
PROJECT_DIR=
VENV_DIR=
PYTHON_BIN=
UVICORN_BIN=